server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    config:
      enabled: false
    gateway:
      server:
        webflux:
          discovery:
            locator:
              enabled: true
              lower-case-service-id: true
          routes:
            - id: patient-service
              uri: lb://patient-service
              predicates:
                - Path=/api/patients/**

            - id: patient-vital-signs
              uri: lb://patient-service
              predicates:
                - Path=/api/vital-signs/**

            - id: doctor-service
              uri: lb://doctor-service
              predicates:
                - Path=/api/doctors/**

            - id: patient-approvals
              uri: lb://doctor-service
              predicates:
                - Path=/api/patient-approvals/**

            - id: vital-signs-review
              uri: lb://doctor-service
              predicates:
                - Path=/api/vital-signs-review/**

            - id: alertes-service
              uri: lb://alertes-service
              predicates:
                - Path=/api/alertes/**

            - id: notifications-route
              uri: lb://alertes-service
              predicates:
                - Path=/api/notifications/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: "*"