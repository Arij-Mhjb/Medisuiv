server:
  port: 8080

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/realms/medisuiv-realm

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
